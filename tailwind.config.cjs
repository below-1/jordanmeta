const config = {
  content: ["./src/**/*.{html,js,svelte,ts}"],

  theme: {
    extend: {
      colors: {
        joyellow: '#facc15',
        joblack: '#402e32',
        jobg: '#fff9ee',
      }
    },
  },

  plugins: [],
};

module.exports = config;
